# Internal tool: keep everything on this machine.
[browser]
gatherUsageStats = false   # don't send usage statistics to Streamlit

[server]
maxUploadSize = 5          # MB, same limit as data_loader.py
